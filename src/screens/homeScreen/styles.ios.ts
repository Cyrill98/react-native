import { StyleSheet } from "react-native";
import globalStyles from "../../globalstyles/defaultStyles";

const styles = StyleSheet.create ({
    root: {
        backgroundColor: globalStyles.color.backgroundLight
    }
})

export default styles